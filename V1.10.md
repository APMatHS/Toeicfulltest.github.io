# TOEIC Full Test — V1.10

V1.10 thay đổi chính sách chống gian lận và bổ sung xem lại toàn bộ bài làm sau khi nộp.

## 1. Chống gian lận

- Rời màn hình được ghi nhận **ngay lập tức** là 1 vi phạm.
- Lần 1 và 2: cảnh báo toàn màn hình, đếm ngược 15 giây để quay lại.
- Quay lại trong 15 giây: tiếp tục làm, số vi phạm không bị xóa.
- Rời quá 15 giây: tự động nộp bài.
- Vi phạm lần thứ 3: tự động nộp ngay.
- Desktop gộp `blur` và `hidden` thành một phiên rời màn hình để tránh cộng trùng.
- Mobile chỉ dùng trạng thái trang `hidden` để tính vi phạm.
- `fullscreen_exit` không tự tạo thêm vi phạm.
- Có âm cảnh báo nếu trình duyệt cho phép phát âm thanh nền.
- Mốc 15 giây được backend kiểm tra theo thời điểm server ghi event, không phụ thuộc hoàn toàn vào timer của tab nền.

## 2. Bảo toàn đáp án khi đang làm

- Mỗi lựa chọn được đưa vào `localStorage` trước khi gửi Supabase.
- Khi mở lại lượt thi, dữ liệu server được nạp trước, sau đó hàng đợi cục bộ được merge để lựa chọn mới nhất không bị mất.
- Khi đổi trạng thái fullscreen, quay lại tab hoặc có mạng trở lại, hệ thống chủ động đồng bộ hàng đợi.
- Có nút **Toàn màn hình** trong giao diện làm bài để vào lại fullscreen bằng thao tác người dùng.
- Trạng thái “Đánh dấu xem lại” tiếp tục được lưu kể cả khi chưa chọn đáp án.

## 3. Xem lại bài sau khi nộp

- Trang kết quả hiển thị lại nội dung đầy đủ của từng câu và phương án sinh viên đã chọn.
- Passage/ảnh/bảng/media được giữ theo bài thi.
- Nếu `show_answers_after_submit = true`: hiển thị đáp án đúng và đúng/sai từng câu.
- Nếu `show_answers_after_submit = false`: chỉ hiển thị bài đã làm và lựa chọn của sinh viên, không trả đáp án đúng hoặc đúng/sai từng câu.
- Bài tự nộp vì chống gian lận hoặc hết giờ vẫn xem lại được.

## 4. Supabase

Sau khi frontend V1.10 đã được triển khai, chạy:

`supabase/v1.10_exam_integrity_and_review.sql`

Migration này có thể chạy trực tiếp khi hệ thống đang ở V1.8 hoặc V1.9.

## 5. Backup

Thư mục `backup/` giữ bản đầy đủ V1.8 và V1.9 trước khi nâng lên V1.10.
