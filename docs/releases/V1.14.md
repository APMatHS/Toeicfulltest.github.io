# TOEIC Full Test V1.14 — Authoring workspace & grading UX

- Trang Soạn đề hiển thị đầy đủ passage/media, câu hỏi và A–D; nhóm Part 6/7 được phân tách rõ.
- Cột điều hướng câu hỏi sticky theo Part, bấm số câu để nhảy nhanh; đánh dấu câu cần kiểm tra.
- Rich editor bật kiểm tra chính tả tiếng Anh của trình duyệt (`spellcheck`, `lang=en`).
- Nút `AI kiểm tra — Bản sau` chỉ hiển thị định hướng, chưa gọi API.
- Danh sách bài kiểm tra tách `Lượt đã làm` thật và `Giới hạn/SV`.
- Kết quả, LIVE, bảng bài làm và Excel chuẩn hóa hiển thị điểm thang 10.
- CSS được tách theo chức năng: core / exam / authoring / review; bỏ file CSS mang tên phiên bản.
- JS bổ sung module `staff-tests.js` và `score-utils.js`; view soạn đề tiếp tục nằm riêng trong `authoring-view.js`.
- Dọn các file backup JS/CSS cũ khỏi cây mã hoạt động; release notes chuyển vào `docs/releases/`.
