# TOEIC Full Test — V1.9

V1.9 tập trung vào độ ổn định khi thi và giảm việc tự nộp quá khắt khe.

- Chống gian lận: chỉ tính khi sinh viên rời tab/cửa sổ **trên 30 giây**.
- Chế độ mặc định: **2 lần đầu cảnh báo, lần thứ 3 tự động nộp**.
- `tab_hidden` và `window_blur` được gộp thành cùng một phiên rời màn hình; thoát fullscreen riêng lẻ không cộng thêm vi phạm.
- Log chống gian lận lưu thêm `duration_seconds`, `left_at`, `returned_at` và nguồn sự kiện trong `details`.
- Đánh dấu “xem lại” của sinh viên được lưu kể cả khi chưa chọn đáp án.
- Làm thử giảng viên dùng hàng đợi lưu tuần tự, tránh mất đáp án khi thao tác nhanh.
- Sau khi kết thúc làm thử, URL chuyển sang route kết quả riêng; refresh không tạo nhầm lượt mới.
- Signed URL media tăng từ 1 giờ lên **6 giờ**, tránh ảnh/audio hết hạn giữa bài thi dài.
- Router bỏ render kép do đồng thời nghe cả `hashchange` và `popstate`.
- Thư mục `backup/` chứa ZIP toàn bộ mã nguồn V1.8 trước khi nâng cấp.

## Supabase

Sau khi đã triển khai frontend V1.9, chạy:

`supabase/v1.9_anti_cheat_and_stability.sql`
