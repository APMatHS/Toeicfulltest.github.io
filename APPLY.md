# Cách áp dụng patch V1.13

Chép đè/thêm đúng các file sau vào root repo `Toeicfulltest/Toeicfulltest.github.io`:

- `index.html` — chép đè.
- `assets/modules/anti-cheat.js` — chép đè.
- `assets/modules/review-ui.js` — file mới.
- `assets/v1.13.css` — file mới.
- `V1.13.md` — file mới.

`README_V1.13_SNIPPET.md` là đoạn gợi ý để đưa lên đầu `README.md`; không bắt buộc cho chức năng.

Không cần chạy SQL hay deploy Edge Function mới.
